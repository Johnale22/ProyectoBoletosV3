version: '3'

services:
  delete-service:
    build: ./delete-service
    ports:
      - "5002:5002"  # Expondrá el puerto 5000 dentro del contenedor al puerto 5001 de la EC2
    environment:
      - DB_HOST=107.21.58.81  # Aquí va la IP pública de la otra EC2 que tendrá la base de datos
      - DB_USER=user
      - DB_PASSWORD=1234
      - DB_NAME=mysqldb